// import { reqCategoryList } from '@/api'
// home模块的小仓库
const state = {};
const mutations = {};
const actions = {
    // 通过Api里面的接口函数调用，向服务器发请求，获取服务器的数据
    categoryList() {
        console.log(111);
    }
};
const getters = {};

export default {
    state,
    mutations,
    actions,
    getters
}